---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

import { supabase } from '../lib/supabase'

let randid=Math.floor(Math.random() * 2) + 1;
console.log(randid)

let { data: uanswers, error } = await supabase
.from('useranswers')
.select('id,useranswerid,uid,answers(id,questionid,answerdesc)')
  .eq('answers.questionid',randid)
  .eq('uid','srhtkrgl@gmail.com')

  let dictionary:any[]=[]
  console.log(uanswers)
---

<Layout title="tarafi.">
{JSON.stringify(dictionary, null, 2) }
<p class="instructions">
			Tarafini seç
		</p>
		<ul role="list" class="link-card-grid">
		
		</ul>
</Layout>
